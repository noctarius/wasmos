#ifndef WASMOS_CTYPE_H
#define WASMOS_CTYPE_H

int tolower(int ch);
int toupper(int ch);
int isspace(int ch);
int isdigit(int ch);
int isalpha(int ch);
int isalnum(int ch);
int isxdigit(int ch);

#endif
