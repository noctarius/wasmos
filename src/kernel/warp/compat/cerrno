#pragma once
/* compat/cerrno — freestanding shadow of <cerrno>
 *
 * Whole content: it includes compat/errno.h, which supplies the errno macro
 * (a constant 0 — see that file for what that costs) and the E* constants.
 *
 * Included by only one WARP source, utils/ExecutableMemory.cpp, which the
 * kernel build does not compile, so nothing in the kernel currently depends on
 * this file beyond making the include resolve. */

#include "errno.h"

/* The errno macro and integer constants are intentionally left in the global
 * namespace (matching the POSIX / C standard convention).  Nothing in WARP
 * uses std::errno, so no namespace std re-export is needed. */
